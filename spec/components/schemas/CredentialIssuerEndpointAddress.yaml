type: string
format: url
description: >-
  URL that points to the credential issuer where the referenced authorizable
  attribute has been already registered
example: 'https://credential.decodeproject.eu'
